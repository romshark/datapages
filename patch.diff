diff --git a/example/classifieds/datapagesgen/action/action_gen.go b/example/classifieds/datapagesgen/action/action_gen.go
index bbdefec..0957e48 100644
--- a/example/classifieds/datapagesgen/action/action_gen.go
+++ b/example/classifieds/datapagesgen/action/action_gen.go
@@ -35,10 +35,8 @@ func POSTPageMessagesRead(query QueryPOSTPageMessagesRead) string {
 		if n > 0 {
 			l += len("&")
 		}
-		n++
 		l += len("msgid=") + len(query.MessageID)
 	}
-	_ = n
 
 	b.Grow(l)
 
@@ -51,8 +49,6 @@ func POSTPageMessagesRead(query QueryPOSTPageMessagesRead) string {
 		if n > 0 {
 			b.WriteString("&")
 		}
-		n++
-		_ = n
 		b.WriteString("msgid=")
 		b.WriteString(query.MessageID)
 	}
diff --git a/generator/testdata/action_app_action_with_query.go.txt b/generator/testdata/action_app_action_with_query.go.txt
index 8d49e54..c1856fc 100644
--- a/generator/testdata/action_app_action_with_query.go.txt
+++ b/generator/testdata/action_app_action_with_query.go.txt
@@ -20,10 +20,8 @@ func POSTAppSearch(query QueryPOSTAppSearch) string {
 		if n > 0 {
 			l += len("&")
 		}
-		n++
 		l += len("t=") + len(query.Term)
 	}
-	_ = n
 
 	b.Grow(l)
 
@@ -36,8 +34,6 @@ func POSTAppSearch(query QueryPOSTAppSearch) string {
 		if n > 0 {
 			b.WriteString("&")
 		}
-		n++
-		_ = n
 		b.WriteString("t=")
 		b.WriteString(query.Term)
 	}
diff --git a/generator/testdata/action_page_path_and_query.go.txt b/generator/testdata/action_page_path_and_query.go.txt
index 735ea77..93e29ab 100644
--- a/generator/testdata/action_page_path_and_query.go.txt
+++ b/generator/testdata/action_page_path_and_query.go.txt
@@ -20,10 +20,8 @@ func POSTPagePostReply(slug string, query QueryPOSTPagePostReply) string {
 		if n > 0 {
 			l += len("&")
 		}
-		n++
 		l += len("pid=") + len(query.ParentID)
 	}
-	_ = n
 
 	b.Grow(l)
 
@@ -38,8 +36,6 @@ func POSTPagePostReply(slug string, query QueryPOSTPagePostReply) string {
 		if n > 0 {
 			b.WriteString("&")
 		}
-		n++
-		_ = n
 		b.WriteString("pid=")
 		b.WriteString(query.ParentID)
 	}
diff --git a/generator/testdata/action_page_query.go.txt b/generator/testdata/action_page_query.go.txt
index 877937c..f22baf9 100644
--- a/generator/testdata/action_page_query.go.txt
+++ b/generator/testdata/action_page_query.go.txt
@@ -20,10 +20,8 @@ func POSTPageMessagesRead(query QueryPOSTPageMessagesRead) string {
 		if n > 0 {
 			l += len("&")
 		}
-		n++
 		l += len("msgid=") + len(query.MessageID)
 	}
-	_ = n
 
 	b.Grow(l)
 
@@ -36,8 +34,6 @@ func POSTPageMessagesRead(query QueryPOSTPageMessagesRead) string {
 		if n > 0 {
 			b.WriteString("&")
 		}
-		n++
-		_ = n
 		b.WriteString("msgid=")
 		b.WriteString(query.MessageID)
 	}
diff --git a/generator/testdata/action_page_query_multiple_fields.go.txt b/generator/testdata/action_page_query_multiple_fields.go.txt
index 9fccce3..d5b374a 100644
--- a/generator/testdata/action_page_query_multiple_fields.go.txt
+++ b/generator/testdata/action_page_query_multiple_fields.go.txt
@@ -28,10 +28,8 @@ func POSTPageMessagesMarkRead(query QueryPOSTPageMessagesMarkRead) string {
 		if n > 0 {
 			l += len("&")
 		}
-		n++
 		l += len("chat=") + len(query.Chat)
 	}
-	_ = n
 
 	b.Grow(l)
 
@@ -45,7 +43,6 @@ func POSTPageMessagesMarkRead(query QueryPOSTPageMessagesMarkRead) string {
 			b.WriteString("&")
 		}
 		n++
-		_ = n
 		b.WriteString("msgid=")
 		b.WriteString(query.MessageID)
 	}
@@ -53,8 +50,6 @@ func POSTPageMessagesMarkRead(query QueryPOSTPageMessagesMarkRead) string {
 		if n > 0 {
 			b.WriteString("&")
 		}
-		n++
-		_ = n
 		b.WriteString("chat=")
 		b.WriteString(query.Chat)
 	}
diff --git a/generator/testdata/action_path_and_query_multiple_fields.go.txt b/generator/testdata/action_path_and_query_multiple_fields.go.txt
index 172259f..612248a 100644
--- a/generator/testdata/action_path_and_query_multiple_fields.go.txt
+++ b/generator/testdata/action_path_and_query_multiple_fields.go.txt
@@ -28,10 +28,8 @@ func POSTPagePostComment(slug string, query QueryPOSTPagePostComment) string {
 		if n > 0 {
 			l += len("&")
 		}
-		n++
 		l += len("draft=") + len(query.Draft)
 	}
-	_ = n
 
 	b.Grow(l)
 
@@ -47,7 +45,6 @@ func POSTPagePostComment(slug string, query QueryPOSTPagePostComment) string {
 			b.WriteString("&")
 		}
 		n++
-		_ = n
 		b.WriteString("pid=")
 		b.WriteString(query.ParentID)
 	}
@@ -55,8 +52,6 @@ func POSTPagePostComment(slug string, query QueryPOSTPagePostComment) string {
 		if n > 0 {
 			b.WriteString("&")
 		}
-		n++
-		_ = n
 		b.WriteString("draft=")
 		b.WriteString(query.Draft)
 	}
diff --git a/generator/testdata/action_path_and_query_multiple_path_vars.go.txt b/generator/testdata/action_path_and_query_multiple_path_vars.go.txt
index 0954cfc..4e3e5d4 100644
--- a/generator/testdata/action_path_and_query_multiple_path_vars.go.txt
+++ b/generator/testdata/action_path_and_query_multiple_path_vars.go.txt
@@ -20,10 +20,8 @@ func POSTPageOrgUpdate(org string, team string, query QueryPOSTPageOrgUpdate) st
 		if n > 0 {
 			l += len("&")
 		}
-		n++
 		l += len("role=") + len(query.Role)
 	}
-	_ = n
 
 	b.Grow(l)
 
@@ -40,8 +38,6 @@ func POSTPageOrgUpdate(org string, team string, query QueryPOSTPageOrgUpdate) st
 		if n > 0 {
 			b.WriteString("&")
 		}
-		n++
-		_ = n
 		b.WriteString("role=")
 		b.WriteString(query.Role)
 	}
diff --git a/generator/testdata/action_path_and_query_with_int_field.go.txt b/generator/testdata/action_path_and_query_with_int_field.go.txt
new file mode 100644
index 0000000..2c9cd03
--- /dev/null
+++ b/generator/testdata/action_path_and_query_with_int_field.go.txt
@@ -0,0 +1,60 @@
+// Generated by github.com/romshark/datapages. DO NOT EDIT.
+
+// Package action provides generators for datastar action hypertext references.
+// These references must be used in the templates.
+package action
+
+import (
+	"strconv"
+	"strings"
+)
+
+// POSTPagePostRate references /post/{slug}/rate/
+func POSTPagePostRate(slug string, query QueryPOSTPagePostRate) string {
+	var (
+		scoreStr string
+	)
+
+	if query.Score != 0 {
+		scoreStr = strconv.FormatInt(query.Score, 10)
+	}
+
+	anyQuery := query.Score != 0
+
+	var b strings.Builder
+	l := len("@post('/post/") + len(slug) + len("/rate/") + len("')")
+	if anyQuery {
+		l += len("?")
+	}
+	n := 0
+	if query.Score != 0 {
+		if n > 0 {
+			l += len("&")
+		}
+		l += len("score=") + len(scoreStr)
+	}
+
+	b.Grow(l)
+
+	b.WriteString("@post('/post/")
+	b.WriteString(slug)
+	b.WriteString("/rate/")
+	if anyQuery {
+		b.WriteString("?")
+	}
+	n = 0
+	if query.Score != 0 {
+		if n > 0 {
+			b.WriteString("&")
+		}
+		b.WriteString("score=")
+		b.WriteString(scoreStr)
+	}
+	b.WriteString("')")
+
+	return b.String()
+}
+
+type QueryPOSTPagePostRate struct {
+	Score int64 `query:"score"`
+}
diff --git a/generator/testdata/action_query_with_int_field.go.txt b/generator/testdata/action_query_with_int_field.go.txt
new file mode 100644
index 0000000..a4d38d7
--- /dev/null
+++ b/generator/testdata/action_query_with_int_field.go.txt
@@ -0,0 +1,58 @@
+// Generated by github.com/romshark/datapages. DO NOT EDIT.
+
+// Package action provides generators for datastar action hypertext references.
+// These references must be used in the templates.
+package action
+
+import (
+	"strconv"
+	"strings"
+)
+
+// POSTPageIndexAdd references /add/
+func POSTPageIndexAdd(query QueryPOSTPageIndexAdd) string {
+	var (
+		deltaStr string
+	)
+
+	if query.Delta != 0 {
+		deltaStr = strconv.FormatInt(query.Delta, 10)
+	}
+
+	anyQuery := query.Delta != 0
+
+	var b strings.Builder
+	l := len("@post('/add/") + len("')")
+	if anyQuery {
+		l += len("?")
+	}
+	n := 0
+	if query.Delta != 0 {
+		if n > 0 {
+			l += len("&")
+		}
+		l += len("delta=") + len(deltaStr)
+	}
+
+	b.Grow(l)
+
+	b.WriteString("@post('/add/")
+	if anyQuery {
+		b.WriteString("?")
+	}
+	n = 0
+	if query.Delta != 0 {
+		if n > 0 {
+			b.WriteString("&")
+		}
+		b.WriteString("delta=")
+		b.WriteString(deltaStr)
+	}
+	b.WriteString("')")
+
+	return b.String()
+}
+
+type QueryPOSTPageIndexAdd struct {
+	Delta int64 `query:"delta"`
+}
diff --git a/generator/write_action.go b/generator/write_action.go
index af1e3df..f3c7b8f 100644
--- a/generator/write_action.go
+++ b/generator/write_action.go
@@ -11,7 +11,7 @@ import (
 // WritePkgAction generates code for the datapagesgen/action package
 // and appends it to buffer.
 func (w *Writer) WritePkgAction(m *model.App) {
-	needsStrings := false
+	needsStrconv, needsStrings := false, false
 
 	// Count total page actions and check if any action uses path or query params.
 	nPageActions := 0
@@ -19,20 +19,27 @@ func (w *Writer) WritePkgAction(m *model.App) {
 		if a.InputPath != nil || a.InputQuery != nil {
 			needsStrings = true
 		}
+		if a.InputQuery != nil {
+			if hasIntFields(w.structFields(a.InputQuery.Type.Resolved)) {
+				needsStrconv = true
+			}
+		}
 	}
 	for _, p := range m.Pages {
 		nPageActions += len(p.Actions)
-		if needsStrings {
-			continue
-		}
 		for _, a := range p.Actions {
 			if a.InputPath != nil || a.InputQuery != nil {
 				needsStrings = true
 			}
+			if a.InputQuery != nil {
+				if hasIntFields(w.structFields(a.InputQuery.Type.Resolved)) {
+					needsStrconv = true
+				}
+			}
 		}
 	}
 
-	w.writeActionHeader(needsStrings)
+	w.writeActionHeader(needsStrconv, needsStrings)
 
 	type actionEntry struct {
 		funcName   string
@@ -81,14 +88,22 @@ func (w *Writer) WritePkgAction(m *model.App) {
 	}
 }
 
-func (w *Writer) writeActionHeader(needsStrings bool) {
+func (w *Writer) writeActionHeader(needsStrconv, needsStrings bool) {
 	w.Line(0, "// Generated by github.com/romshark/datapages. DO NOT EDIT.")
 	w.Line(0, "")
 	w.Line(0, "// Package action provides generators for datastar action hypertext references.")
 	w.Line(0, "// These references must be used in the templates.")
 	w.Line(0, "package action")
 	w.Line(0, "")
-	if needsStrings {
+	switch {
+	case needsStrconv && needsStrings:
+		w.Line(0, "import (")
+		w.Line(1, `"strconv"`)
+		w.Line(1, `"strings"`)
+		w.Line(0, ")")
+	case needsStrconv:
+		w.Line(0, `import "strconv"`)
+	case needsStrings:
 		w.Line(0, `import "strings"`)
 	}
 }
@@ -222,6 +237,9 @@ func (w *Writer) writeActionFuncQueryOnly(
 	w.Raw(funcName)
 	w.Raw(") string {\n")
 
+	// Pre-convert int fields to strings.
+	w.writeIntPreConvert(fields)
+
 	// anyQuery check.
 	w.writeAnyCheck("anyQuery", fields)
 	w.Line(0, "")
@@ -239,21 +257,22 @@ func (w *Writer) writeActionFuncQueryOnly(
 
 	// Query param length accumulation.
 	w.Line(1, "n := 0")
-	for _, f := range fields {
+	for i, f := range fields {
 		tag := queryTagValue(f.Tag)
 		w.writeIfZeroCheck(1, "query."+f.Name, f.Type)
 		w.Line(2, "if n > 0 {")
 		w.Line(3, `l += len("&")`)
 		w.Line(2, "}")
-		w.Line(2, "n++")
-		w.Raw("\t\tl += len(\"")
-		w.Raw(tag)
-		w.Raw("=\") + len(query.")
-		w.Raw(f.Name)
-		w.Raw(")\n")
+		if i < len(fields)-1 {
+			w.Line(2, "n++")
+		}
+		if isIntType(f.Type) {
+			w.Linef(2, "l += len(%q) + len(%sStr)", tag+"=", tag)
+		} else {
+			w.Linef(2, "l += len(%q) + len(query.%s)", tag+"=", f.Name)
+		}
 		w.Line(1, "}")
 	}
-	w.Line(1, "_ = n")
 	w.Line(0, "")
 
 	// Grow and write.
@@ -270,20 +289,21 @@ func (w *Writer) writeActionFuncQueryOnly(
 
 	// Write query params.
 	w.Line(1, "n = 0")
-	for _, f := range fields {
+	for i, f := range fields {
 		tag := queryTagValue(f.Tag)
 		w.writeIfZeroCheck(1, "query."+f.Name, f.Type)
 		w.Line(2, "if n > 0 {")
 		w.Line(3, `b.WriteString("&")`)
 		w.Line(2, "}")
-		w.Line(2, "n++")
-		w.Line(2, "_ = n")
-		w.Raw("\t\tb.WriteString(\"")
-		w.Raw(tag)
-		w.Raw("=\")\n")
-		w.Raw("\t\tb.WriteString(query.")
-		w.Raw(f.Name)
-		w.Raw(")\n")
+		if i < len(fields)-1 {
+			w.Line(2, "n++")
+		}
+		w.Linef(2, "b.WriteString(%q)", tag+"=")
+		if isIntType(f.Type) {
+			w.Linef(2, "b.WriteString(%sStr)", tag)
+		} else {
+			w.Linef(2, "b.WriteString(query.%s)", f.Name)
+		}
 		w.Line(1, "}")
 	}
 
@@ -311,6 +331,9 @@ func (w *Writer) writeActionFuncPathAndQuery(
 	w.Raw(funcName)
 	w.Raw(") string {\n")
 
+	// Pre-convert int fields to strings.
+	w.writeIntPreConvert(fields)
+
 	// anyQuery check.
 	w.writeAnyCheck("anyQuery", fields)
 	w.Line(0, "")
@@ -339,21 +362,22 @@ func (w *Writer) writeActionFuncPathAndQuery(
 
 	// Query param length accumulation.
 	w.Line(1, "n := 0")
-	for _, f := range fields {
+	for i, f := range fields {
 		tag := queryTagValue(f.Tag)
 		w.writeIfZeroCheck(1, "query."+f.Name, f.Type)
 		w.Line(2, "if n > 0 {")
 		w.Line(3, `l += len("&")`)
 		w.Line(2, "}")
-		w.Line(2, "n++")
-		w.Raw("\t\tl += len(\"")
-		w.Raw(tag)
-		w.Raw("=\") + len(query.")
-		w.Raw(f.Name)
-		w.Raw(")\n")
+		if i < len(fields)-1 {
+			w.Line(2, "n++")
+		}
+		if isIntType(f.Type) {
+			w.Linef(2, "l += len(%q) + len(%sStr)", tag+"=", tag)
+		} else {
+			w.Linef(2, "l += len(%q) + len(query.%s)", tag+"=", f.Name)
+		}
 		w.Line(1, "}")
 	}
-	w.Line(1, "_ = n")
 	w.Line(0, "")
 
 	// Grow and write path segments.
@@ -379,20 +403,21 @@ func (w *Writer) writeActionFuncPathAndQuery(
 
 	// Write query params.
 	w.Line(1, "n = 0")
-	for _, f := range fields {
+	for i, f := range fields {
 		tag := queryTagValue(f.Tag)
 		w.writeIfZeroCheck(1, "query."+f.Name, f.Type)
 		w.Line(2, "if n > 0 {")
 		w.Line(3, `b.WriteString("&")`)
 		w.Line(2, "}")
-		w.Line(2, "n++")
-		w.Line(2, "_ = n")
-		w.Raw("\t\tb.WriteString(\"")
-		w.Raw(tag)
-		w.Raw("=\")\n")
-		w.Raw("\t\tb.WriteString(query.")
-		w.Raw(f.Name)
-		w.Raw(")\n")
+		if i < len(fields)-1 {
+			w.Line(2, "n++")
+		}
+		w.Linef(2, "b.WriteString(%q)", tag+"=")
+		if isIntType(f.Type) {
+			w.Linef(2, "b.WriteString(%sStr)", tag)
+		} else {
+			w.Linef(2, "b.WriteString(query.%s)", f.Name)
+		}
 		w.Line(1, "}")
 	}
 
diff --git a/generator/write_action_test.go b/generator/write_action_test.go
index 66efd5d..fc00ce4 100644
--- a/generator/write_action_test.go
+++ b/generator/write_action_test.go
@@ -1,7 +1,11 @@
 package generator_test
 
 import (
+	"go/ast"
 	"go/format"
+	"go/importer"
+	goparser "go/parser"
+	"go/token"
 	"go/types"
 	"os"
 	"path/filepath"
@@ -179,6 +183,33 @@ func TestWritePkgAction(t *testing.T) {
 			}},
 			golden: "action_path_and_query_multiple_fields.go.txt",
 		},
+		"query with int field": {
+			app: &model.App{Pages: []*model.Page{
+				actionPage("PageIndex",
+					actionHandler("post", "Add", "/add/{$}",
+						hrefStruct(
+							hrefFieldDef{
+								"Delta", types.Typ[types.Int64],
+								`query:"delta"`,
+							},
+						))),
+			}},
+			golden: "action_query_with_int_field.go.txt",
+		},
+		"path and query with int field": {
+			app: &model.App{Pages: []*model.Page{
+				actionPage("PagePost",
+					actionHandler("post", "Rate",
+						"/post/{slug}/rate/{$}",
+						hrefStruct(
+							hrefFieldDef{
+								"Score", types.Typ[types.Int64],
+								`query:"score"`,
+							},
+						))),
+			}},
+			golden: "action_path_and_query_with_int_field.go.txt",
+		},
 		"path and query multiple path vars": {
 			app: &model.App{Pages: []*model.Page{
 				actionPage("PageOrg",
@@ -219,6 +250,55 @@ func TestWritePkgAction(t *testing.T) {
 	}
 }
 
+func TestWritePkgActionIntFieldTypeChecks(t *testing.T) {
+	tests := map[string]*model.App{
+		"query only with int field": {Pages: []*model.Page{
+			actionPage("PageIndex",
+				actionHandler("post", "Add", "/add/{$}",
+					hrefStruct(
+						hrefFieldDef{
+							"Delta", types.Typ[types.Int64],
+							`query:"delta"`,
+						},
+					))),
+		}},
+		"path and query with int field": {Pages: []*model.Page{
+			actionPage("PagePost",
+				actionHandler("post", "Rate", "/post/{slug}/rate/{$}",
+					hrefStruct(
+						hrefFieldDef{
+							"Score", types.Typ[types.Int64],
+							`query:"score"`,
+						},
+					))),
+		}},
+	}
+
+	w := generator.Writer{Buf: make([]byte, 2*1024*1024)}
+	for name, app := range tests {
+		t.Run(name, func(t *testing.T) {
+			w.Reset()
+			w.WritePkgAction(app)
+			src, err := format.Source(w.Buf)
+			require.NoError(t, err,
+				"generated code is not valid Go syntax:\n%s", string(w.Buf))
+			typeCheckGenerated(t, src)
+		})
+	}
+}
+
+func typeCheckGenerated(t *testing.T, src []byte) {
+	t.Helper()
+	fset := token.NewFileSet()
+	f, err := goparser.ParseFile(fset, "gen.go", src, 0)
+	require.NoError(t, err, "parse error")
+
+	conf := types.Config{Importer: importer.Default()}
+	_, err = conf.Check("action", fset, []*ast.File{f}, nil)
+	require.NoError(t, err,
+		"generated code does not type-check:\n%s", string(src))
+}
+
 // actionPage constructs a *model.Page with the given actions.
 func actionPage(typeName string, actions ...*model.Handler) *model.Page {
 	return &model.Page{
diff --git a/generator/write_href.go b/generator/write_href.go
index e88648f..755ba7d 100644
--- a/generator/write_href.go
+++ b/generator/write_href.go
@@ -227,41 +227,8 @@ func (w *Writer) writeHrefFuncQueryOnly(
 ) {
 	w.Linef(0, "func %s(query Query%s) string {", funcName, funcName)
 
-	// Pre-convert int64 fields to strings.
-	if hasIntFields(fields) {
-		w.Line(1, "var (")
-		for _, f := range fields {
-			if isIntType(f.Type) {
-				tag := queryTagValue(f.Tag)
-				w.Linef(2, "%sStr string", tag)
-			}
-		}
-		w.Line(1, ")")
-		w.Line(0, "")
-		for _, f := range fields {
-			if isIntType(f.Type) {
-				tag := queryTagValue(f.Tag)
-				_, unsigned := intTypeParseInfo(f.Type)
-				typeName := intTypeName(f.Type)
-				w.Linef(1, "if query.%s != 0 {", f.Name)
-				if unsigned {
-					if typeName != "uint64" {
-						w.Linef(2, "%sStr = strconv.FormatUint(uint64(query.%s), 10)", tag, f.Name)
-					} else {
-						w.Linef(2, "%sStr = strconv.FormatUint(query.%s, 10)", tag, f.Name)
-					}
-				} else {
-					if typeName != "int64" {
-						w.Linef(2, "%sStr = strconv.FormatInt(int64(query.%s), 10)", tag, f.Name)
-					} else {
-						w.Linef(2, "%sStr = strconv.FormatInt(query.%s, 10)", tag, f.Name)
-					}
-				}
-				w.Line(1, "}")
-			}
-		}
-		w.Line(0, "")
-	}
+	// Pre-convert int fields to strings.
+	w.writeIntPreConvert(fields)
 
 	// anyQuery check.
 	w.writeAnyCheck("anyQuery", fields)
@@ -354,41 +321,8 @@ func (w *Writer) writeHrefFuncPathAndQuery(
 
 	literals, _ := routeSegments(route)
 
-	// Pre-convert int64 fields to strings.
-	if hasIntFields(fields) {
-		w.Line(1, "var (")
-		for _, f := range fields {
-			if isIntType(f.Type) {
-				tag := queryTagValue(f.Tag)
-				w.Linef(2, "%sStr string", tag)
-			}
-		}
-		w.Line(1, ")")
-		w.Line(0, "")
-		for _, f := range fields {
-			if isIntType(f.Type) {
-				tag := queryTagValue(f.Tag)
-				_, unsigned := intTypeParseInfo(f.Type)
-				typeName := intTypeName(f.Type)
-				w.Linef(1, "if query.%s != 0 {", f.Name)
-				if unsigned {
-					if typeName != "uint64" {
-						w.Linef(2, "%sStr = strconv.FormatUint(uint64(query.%s), 10)", tag, f.Name)
-					} else {
-						w.Linef(2, "%sStr = strconv.FormatUint(query.%s, 10)", tag, f.Name)
-					}
-				} else {
-					if typeName != "int64" {
-						w.Linef(2, "%sStr = strconv.FormatInt(int64(query.%s), 10)", tag, f.Name)
-					} else {
-						w.Linef(2, "%sStr = strconv.FormatInt(query.%s, 10)", tag, f.Name)
-					}
-				}
-				w.Line(1, "}")
-			}
-		}
-		w.Line(0, "")
-	}
+	// Pre-convert int fields to strings.
+	w.writeIntPreConvert(fields)
 
 	// anyQuery check.
 	w.writeAnyCheck("anyQuery", fields)
@@ -552,11 +486,51 @@ func (w *Writer) writeIfZeroCheck(indent int, expr string, t types.Type) {
 // fieldTypeName returns the Go type name for a field type.
 func fieldTypeName(t types.Type) string {
 	if isIntType(t) {
-		return "int64"
+		return intTypeName(t)
 	}
 	return "string"
 }
 
+// writeIntPreConvert emits variable declarations and conversion code
+// for int fields (e.g. "deltaStr = strconv.FormatInt(query.Delta, 10)").
+func (w *Writer) writeIntPreConvert(fields []structFieldInfo) {
+	if !hasIntFields(fields) {
+		return
+	}
+	w.Line(1, "var (")
+	for _, f := range fields {
+		if isIntType(f.Type) {
+			tag := queryTagValue(f.Tag)
+			w.Linef(2, "%sStr string", tag)
+		}
+	}
+	w.Line(1, ")")
+	w.Line(0, "")
+	for _, f := range fields {
+		if isIntType(f.Type) {
+			tag := queryTagValue(f.Tag)
+			_, unsigned := intTypeParseInfo(f.Type)
+			typeName := intTypeName(f.Type)
+			w.Linef(1, "if query.%s != 0 {", f.Name)
+			if unsigned {
+				if typeName != "uint64" {
+					w.Linef(2, "%sStr = strconv.FormatUint(uint64(query.%s), 10)", tag, f.Name)
+				} else {
+					w.Linef(2, "%sStr = strconv.FormatUint(query.%s, 10)", tag, f.Name)
+				}
+			} else {
+				if typeName != "int64" {
+					w.Linef(2, "%sStr = strconv.FormatInt(int64(query.%s), 10)", tag, f.Name)
+				} else {
+					w.Linef(2, "%sStr = strconv.FormatInt(query.%s, 10)", tag, f.Name)
+				}
+			}
+			w.Line(1, "}")
+		}
+	}
+	w.Line(0, "")
+}
+
 // hasIntFields reports whether any field is an int/int64 type.
 func hasIntFields(fields []structFieldInfo) bool {
 	for _, f := range fields {
