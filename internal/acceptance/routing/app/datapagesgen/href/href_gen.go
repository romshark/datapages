// Code generated by github.com/romshark/datapages; DO NOT EDIT.

// Package href provides generators for datastar anchor hypertext references.
// Use these in templates instead of hardcoding URLs.
package href

import (
	"log/slog"
	"net/url"
	"strconv"
	"strings"
	"sync/atomic"

	"github.com/romshark/datapages/runtime/hrefcheck"
)

var logger atomic.Pointer[slog.Logger]

func init() { logger.Store(slog.Default()) }

// SetLogger sets the logger used by External to log invalid URLs.
// Passing nil resets to the default logger. It is safe for concurrent use.
func SetLogger(l *slog.Logger) {
	if l == nil {
		l = slog.Default()
	}
	logger.Store(l)
}

func getLogger() *slog.Logger { return logger.Load() }

// External returns url as-is for use in href attributes.
// It logs a warning at runtime if the URL is not an allowed
// non-relative href (e.g. app-internal paths, javascript:, relative URLs).
func External(url string) string {
	if !hrefcheck.IsAllowedNonRelativeHref(url) {
		getLogger().Warn("href.External called with app-internal URL", "url", url)
	}
	return url
}

// PageConflict references /c/{value}/{s_value}/{s_s_value}/{$}
func PageConflict(value int32, s_value int32, s_s_value string) string {
	s_s_s_value := strconv.FormatInt(int64(value), 10)
	s_s_s_s_value := strconv.FormatInt(int64(s_value), 10)
	s_s_s_s_s_value := url.PathEscape(s_s_value)
	var b strings.Builder
	b.Grow(
		len("/c/") +
			len(s_s_s_value) +
			len("/") +
			len(s_s_s_s_value) +
			len("/") +
			len(s_s_s_s_s_value) +
			len("/"),
	)
	b.WriteString("/c/")
	b.WriteString(s_s_s_value)
	b.WriteString("/")
	b.WriteString(s_s_s_s_value)
	b.WriteString("/")
	b.WriteString(s_s_s_s_s_value)
	b.WriteString("/")
	return b.String()
}

// PageFiles references /files/{rest...}/{$}
func PageFiles(rest string) string {
	s_rest := url.PathEscape(rest)
	var b strings.Builder
	b.Grow(
		len("/files/") +
			len(s_rest) +
			len("/"),
	)
	b.WriteString("/files/")
	b.WriteString(s_rest)
	b.WriteString("/")
	return b.String()
}

// PageIndex references /{$}
func PageIndex() string { return "/" }

// PageInts references /ints/{i8}/{i16}/{i32}/{i64}/{u8}/{u16}/{u32}/{$}
func PageInts(i8 int8, i16 int16, i32 int32, i64 int64, u8 uint8, u16 uint16, u32 uint32) string {
	s_i8 := strconv.FormatInt(int64(i8), 10)
	s_i16 := strconv.FormatInt(int64(i16), 10)
	s_i32 := strconv.FormatInt(int64(i32), 10)
	s_i64 := strconv.FormatInt(i64, 10)
	s_u8 := strconv.FormatUint(uint64(u8), 10)
	s_u16 := strconv.FormatUint(uint64(u16), 10)
	s_u32 := strconv.FormatUint(uint64(u32), 10)
	var b strings.Builder
	b.Grow(
		len("/ints/") +
			len(s_i8) +
			len("/") +
			len(s_i16) +
			len("/") +
			len(s_i32) +
			len("/") +
			len(s_i64) +
			len("/") +
			len(s_u8) +
			len("/") +
			len(s_u16) +
			len("/") +
			len(s_u32) +
			len("/"),
	)
	b.WriteString("/ints/")
	b.WriteString(s_i8)
	b.WriteString("/")
	b.WriteString(s_i16)
	b.WriteString("/")
	b.WriteString(s_i32)
	b.WriteString("/")
	b.WriteString(s_i64)
	b.WriteString("/")
	b.WriteString(s_u8)
	b.WriteString("/")
	b.WriteString(s_u16)
	b.WriteString("/")
	b.WriteString(s_u32)
	b.WriteString("/")
	return b.String()
}

// PageMixed references /org/{org}/item/{id}/{$}
func PageMixed(org string, id int, query QueryPageMixed) string {
	s_org := url.PathEscape(org)
	s_id := strconv.FormatInt(int64(id), 10)
	var (
		tabStr  string
		pageStr string
	)

	if query.Tab != "" {
		tabStr = url.QueryEscape(query.Tab)
	}
	if query.Page != 0 {
		pageStr = strconv.FormatInt(int64(query.Page), 10)
	}

	anyQuery := query.Tab != "" ||
		query.Page != 0

	var b strings.Builder
	l := len("/org/") +
		len(s_org) +
		len("/item/") +
		len(s_id) +
		len("/")
	if anyQuery {
		l += len("?")
	}

	// n = number of query params already accounted for (for '&')
	n := 0

	if query.Tab != "" {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("tab=") + len(tabStr)
	}
	if query.Page != 0 {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("page=") + len(pageStr)
	}
	_ = n

	b.Grow(l)

	b.WriteString("/org/")
	b.WriteString(s_org)
	b.WriteString("/item/")
	b.WriteString(s_id)
	b.WriteString("/")
	if anyQuery {
		b.WriteString("?")
	}

	n = 0

	if query.Tab != "" {
		if n > 0 {
			b.WriteString("&")
		}
		n++
		b.WriteString("tab=")
		b.WriteString(tabStr)
	}
	if query.Page != 0 {
		if n > 0 {
			b.WriteString("&")
		}
		b.WriteString("page=")
		b.WriteString(pageStr)
	}

	return b.String()
}

// QueryPageMixed is the query parameters for PageMixed
type QueryPageMixed struct {
	Tab  string `query:"tab"`
	Page int    `query:"page"`
}

// PagePath references /p/{str}/{i}/{u}/{f}/{flag}/{$}
func PagePath(str string, i int, u uint64, f float64, flag bool) string {
	s_str := url.PathEscape(str)
	s_i := strconv.FormatInt(int64(i), 10)
	s_u := strconv.FormatUint(u, 10)
	s_f := strconv.FormatFloat(f, 'f', -1, 64)
	s_flag := strconv.FormatBool(flag)
	var b strings.Builder
	b.Grow(
		len("/p/") +
			len(s_str) +
			len("/") +
			len(s_i) +
			len("/") +
			len(s_u) +
			len("/") +
			len(s_f) +
			len("/") +
			len(s_flag) +
			len("/"),
	)
	b.WriteString("/p/")
	b.WriteString(s_str)
	b.WriteString("/")
	b.WriteString(s_i)
	b.WriteString("/")
	b.WriteString(s_u)
	b.WriteString("/")
	b.WriteString(s_f)
	b.WriteString("/")
	b.WriteString(s_flag)
	b.WriteString("/")
	return b.String()
}

// PageQuery references /q/{$}
func PageQuery(query QueryPageQuery) string {
	var (
		termStr  string
		limitStr string
		ratioStr string
		scoreStr string
		bigStr   string
		deepStr  string
		flagStr  string
	)

	if query.Term != "" {
		termStr = url.QueryEscape(query.Term)
	}
	if query.Limit != 0 {
		limitStr = strconv.FormatInt(int64(query.Limit), 10)
	}
	if query.Ratio != 0 {
		ratioStr = strconv.FormatFloat(float64(query.Ratio), 'f', -1, 32)
	}
	if query.Score != 0 {
		scoreStr = strconv.FormatFloat(query.Score, 'f', -1, 64)
	}
	if query.Big != 0 {
		bigStr = strconv.FormatUint(uint64(query.Big), 10)
	}
	if query.Deep != 0 {
		deepStr = strconv.FormatInt(query.Deep, 10)
	}
	if query.Flag {
		flagStr = strconv.FormatBool(query.Flag)
	}

	anyQuery := query.Term != "" ||
		query.Limit != 0 ||
		query.Ratio != 0 ||
		query.Score != 0 ||
		query.Big != 0 ||
		query.Deep != 0 ||
		query.Flag

	var b strings.Builder
	l := len("/q/")
	if anyQuery {
		l += len("?")
	}

	// n = number of query params already accounted for (for '&')
	n := 0

	if query.Term != "" {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("term=") + len(termStr)
	}
	if query.Limit != 0 {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("limit=") + len(limitStr)
	}
	if query.Ratio != 0 {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("ratio=") + len(ratioStr)
	}
	if query.Score != 0 {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("score=") + len(scoreStr)
	}
	if query.Big != 0 {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("big=") + len(bigStr)
	}
	if query.Deep != 0 {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("deep=") + len(deepStr)
	}
	if query.Flag {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("flag=") + len(flagStr)
	}
	_ = n

	b.Grow(l)

	b.WriteString("/q/")
	if anyQuery {
		b.WriteString("?")
	}

	n = 0

	if query.Term != "" {
		if n > 0 {
			b.WriteString("&")
		}
		n++
		b.WriteString("term=")
		b.WriteString(termStr)
	}
	if query.Limit != 0 {
		if n > 0 {
			b.WriteString("&")
		}
		n++
		b.WriteString("limit=")
		b.WriteString(limitStr)
	}
	if query.Ratio != 0 {
		if n > 0 {
			b.WriteString("&")
		}
		n++
		b.WriteString("ratio=")
		b.WriteString(ratioStr)
	}
	if query.Score != 0 {
		if n > 0 {
			b.WriteString("&")
		}
		n++
		b.WriteString("score=")
		b.WriteString(scoreStr)
	}
	if query.Big != 0 {
		if n > 0 {
			b.WriteString("&")
		}
		n++
		b.WriteString("big=")
		b.WriteString(bigStr)
	}
	if query.Deep != 0 {
		if n > 0 {
			b.WriteString("&")
		}
		n++
		b.WriteString("deep=")
		b.WriteString(deepStr)
	}
	if query.Flag {
		if n > 0 {
			b.WriteString("&")
		}
		b.WriteString("flag=")
		b.WriteString(flagStr)
	}

	return b.String()
}

// QueryPageQuery is the query parameters for PageQuery
type QueryPageQuery struct {
	Term  string  `query:"term"`
	Limit int     `query:"limit"`
	Ratio float32 `query:"ratio"`
	Score float64 `query:"score"`
	Big   uint32  `query:"big"`
	Deep  int64   `query:"deep"`
	Flag  bool    `query:"flag"`
}

// PageReflect references /reflect/{$}
func PageReflect(query QueryPageReflect) string {
	var (
		tStr string
		pStr string
	)

	if query.Term != "" {
		tStr = url.QueryEscape(query.Term)
	}
	if query.Page != 0 {
		pStr = strconv.FormatInt(int64(query.Page), 10)
	}

	anyQuery := query.Term != "" ||
		query.Page != 0

	var b strings.Builder
	l := len("/reflect/")
	if anyQuery {
		l += len("?")
	}

	// n = number of query params already accounted for (for '&')
	n := 0

	if query.Term != "" {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("t=") + len(tStr)
	}
	if query.Page != 0 {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("p=") + len(pStr)
	}
	_ = n

	b.Grow(l)

	b.WriteString("/reflect/")
	if anyQuery {
		b.WriteString("?")
	}

	n = 0

	if query.Term != "" {
		if n > 0 {
			b.WriteString("&")
		}
		n++
		b.WriteString("t=")
		b.WriteString(tStr)
	}
	if query.Page != 0 {
		if n > 0 {
			b.WriteString("&")
		}
		b.WriteString("p=")
		b.WriteString(pStr)
	}

	return b.String()
}

// QueryPageReflect is the query parameters for PageReflect
type QueryPageReflect struct {
	Term string `query:"t"`
	Page int    `query:"p"`
}

// PageSlug references /slug/{slug}/{$}
func PageSlug(slug string, query QueryPageSlug) string {
	s_slug := url.PathEscape(slug)
	var (
		tagStr string
	)

	if query.Tag != "" {
		tagStr = url.QueryEscape(query.Tag)
	}

	anyQuery := query.Tag != ""

	var b strings.Builder
	l := len("/slug/") +
		len(s_slug) +
		len("/")
	if anyQuery {
		l += len("?")
	}

	// n = number of query params already accounted for (for '&')
	n := 0

	if query.Tag != "" {
		if n > 0 {
			l += len("&")
		}
		n++
		l += len("tag=") + len(tagStr)
	}
	_ = n

	b.Grow(l)

	b.WriteString("/slug/")
	b.WriteString(s_slug)
	b.WriteString("/")
	if anyQuery {
		b.WriteString("?")
	}

	n = 0

	if query.Tag != "" {
		if n > 0 {
			b.WriteString("&")
		}
		b.WriteString("tag=")
		b.WriteString(tagStr)
	}

	return b.String()
}

// QueryPageSlug is the query parameters for PageSlug
type QueryPageSlug struct {
	Tag string `query:"tag"`
}

// PageTitled references /titled/{name}/{$}
func PageTitled(name string) string {
	s_name := url.PathEscape(name)
	var b strings.Builder
	b.Grow(
		len("/titled/") +
			len(s_name) +
			len("/"),
	)
	b.WriteString("/titled/")
	b.WriteString(s_name)
	b.WriteString("/")
	return b.String()
}
