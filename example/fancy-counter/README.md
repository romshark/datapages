# Fancy Counter

A polished real-time collaborative counter with number-flow animations,
min/max/set controls, and styled UI.

For a minimal bare-bones version, see [`counter`](../counter/).

## Run

```sh
go run ./cmd/server
```

Then open http://localhost:8080/.
