// Package httpserve holds the HTTP server of a generated Datapages server:
// the listener, the routes, the middleware chain, the logger and the shutdown,
// plus the redirect, the Datastar request check and the dev-mode cache headers.
//
// Application code must not import this package.
package httpserve
